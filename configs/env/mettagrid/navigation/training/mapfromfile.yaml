defaults:
  - /env/mettagrid-base/mettagrid@

game:
  num_agents: 20  #how many agents are in the map x2

  map_builder:

   map_builder:
    _target_: mettagrid.config.room.multi_room.MultiRoom
    num_rooms: 2
    border_width: 6

    room:
      _target_: mettagrid.config.room.map_from_file.MapFromFile
      border_width: 1
      dir: "terrain_maps_nohearts"

  objects:
    altar:
      cooldown: 255

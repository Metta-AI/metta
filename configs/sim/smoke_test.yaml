defaults:
  - simulation_base
  - _self_

# override simulation_base
max_time_s: 10
num_episodes: 1

name: smoke_test

simulations:
  emptyspace_withinsight:
    env: env/mettagrid/navigation/evals/emptyspace_withinsight

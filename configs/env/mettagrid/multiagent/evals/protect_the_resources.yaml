defaults:
  - /env/mettagrid/multiagent/evals/defaults@
  - _self_

game:
  num_agents: 6 #how many agents are in the map x2
  max_steps: 130
  agent:
    rewards:
      heart: 0.3333

  map_builder:
    uri: "configs/env/mettagrid/maps/multiagent/protect_the_resources.map"

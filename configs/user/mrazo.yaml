# @package __global__

defaults:
  - override /agent: simple_with_memory
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/t_maze
  wandb.enabled: true
  wandb.track: true

# policy_uri: wandb://run/mrazo_memory-traces_navigation_v01

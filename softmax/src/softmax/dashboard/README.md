## Overview

This module provides the metric registry for Softmax system health metrics.

## Usage

Metrics are registered using the `@system_health_metric` decorator and collected via `collect_metrics()`.

defaults:
  - /env/mettagrid/memory/evals/defaults@

game:
  max_steps: 600
  map_builder:
    uri: "configs/env/mettagrid/maps/memory_tests/memory_swirls_hard.map"
  objects:
    altar:
      initial_items: 1

defaults:
  - mettagrid
  - game/objects:
      - basic
      - mines
      - generators
      - combat
      - advanced

  - _self_

game:
  num_agents: 24
  map_builder:
    _target_: metta.mettagrid.room.multi_room.MultiRoom
    num_rooms: ${div:${..num_agents},6}
    border_width: 6

    room:
      _target_: metta.mettagrid.room.terrain_from_numpy.TerrainFromNumpy
      border_width: 0

      agents: 6

      objects:
        mine_red: 10  # Fixed value (was 10  # Fixed value (was ${sampling:1,20,10}))
        generator_red: 2  # Fixed value (was 2  # Fixed value (was ${sampling:1,10,2}))
        altar: 1  # Fixed value (was 1  # Fixed value (was ${sampling:1,5,1}))
        armory: 1  # Fixed value (was 1  # Fixed value (was ${sampling:1,5,1}))
        lasery: 1  # Fixed value (was 1  # Fixed value (was ${sampling:1,5,1}))
        lab: 1  # Fixed value (was 1  # Fixed value (was ${sampling:1,5,1}))
        factory: 1  # Fixed value (was 1  # Fixed value (was ${sampling:1,5,1}))
        temple: 1  # Fixed value (was 1  # Fixed value (was ${sampling:1,5,1}))

        block: 20  # Fixed value (was 20  # Fixed value (was ${sampling:5,50,20}))
        wall: 20  # Fixed value (was 20  # Fixed value (was ${sampling:5,50,20}))

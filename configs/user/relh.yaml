# @package __global__

defaults:
  # - override /env/mettagrid@env: simple
  # - override /agent: non_robust_puffer
  - _self_

run: eval.metta.new
run_dir: ${data_dir}/${run}

policy_uri: pytorch://checkpoints/metta_6-8/metta_6-8.pt
npc_policy_uri: null

pytorch:
 _target_: metta.agent.external.torch.Recurrent
 hidden_size: 512

trained_policy_uri: pytorch://checkpoints/metta_6-8/metta_6-8.pt

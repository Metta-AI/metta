name: bbc_agents_1
algorithm: learning_progress

# Inherit from defaults
defaults:
  - defaults

env_overrides:
  game:
    map_builder:
      instances: 24
      root:
        params:
          agents: 1
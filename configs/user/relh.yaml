# @package __global__

defaults:
  - _self_

pytorch:
 _target_: metta.agent.external.latent_attn_med.Recurrent
 hidden_size: 512
 cnn_channels: 128

trainer:
  initial_policy:
    uri: pytorch://checkpoints/metta-new/metta.pt

trained_policy_uri: pytorch://checkpoints/metta-new/metta.pt

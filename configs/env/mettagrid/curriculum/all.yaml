name: all_curricula
algorithm:
  type: discrete_random
  params:
    initial_weights: [5, 3, 2]

children:
  - /env/mettagrid/curriculum/arena/learning_progress
  - /env/mettagrid/curriculum/navigation/learning_progress
  - /env/mettagrid/curriculum/object_use

env_overrides:
  game:
    num_agents: 24

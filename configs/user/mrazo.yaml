# @package __global__

defaults:
  - override /agent: robust_cross
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/object_use
  wandb.enabled: true
  wandb.track: true

run: mrazo_act-obs_objectuse_robust-cross_v01

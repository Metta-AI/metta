# Database configuration
STATS_DB_URI=postgres://postgres:password@postgres:5432/metta_stats

# Debug user email (for local development without oauth2_proxy)
DEBUG_USER_EMAIL=test@example.com

# PostgreSQL settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=metta_stats
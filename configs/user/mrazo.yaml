# @package __global__

defaults:
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/navigation
  wandb.enabled: false
  wandb.track: false

# replay_job:
#   policy_uri: wandb://run/mrazo_action-obs_memory_test01
#   wandb.enabled: false
#   wandb.track: false

run: mrazo_action-obs_navigation_v001

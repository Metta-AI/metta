cmake_minimum_required(VERSION 3.15)
project(metta)

# This is a placeholder CMakeLists.txt file for the main metta project.
# The actual C++ extensions are built in the mettagrid subdirectory.
# This file exists to satisfy scikit-build-core's requirement for a CMakeLists.txt
# in the root directory when using the scikit-build-core build backend.

# No targets to build in the root directory
message(STATUS "Metta root CMakeLists.txt - no targets to build")

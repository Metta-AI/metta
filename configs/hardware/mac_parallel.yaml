# @package __global__

device: cpu

vectorization: multiprocessing

trainer:
  num_workers: 4
  update_epochs: 1
  batch_size: 1024
  minibatch_size: 1024
  forward_pass_minibatch_target_size: 512
  async_factor: 1
  checkpoint_interval: 10
  bptt_horizon: 8
  num_steps: 32
# Config: Two Rooms Coord (Overcooked-style)
# This is a template that should be filled in by either a curriculum or a sim.
defaults:
  - /env/mettagrid/cooperation/experimental/two_rooms_coord_template@
  - _self_

game:
  # #override_params
  params:
    width: 5
    height: 5
    num_shared_generators: 1
    num_altars: 1
    num_mines: 1

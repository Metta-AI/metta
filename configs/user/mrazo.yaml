# @package __global__

defaults:
  - override /agent: simple_with_memory
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/object_use
  wandb.enabled: true
  wandb.track: true

run: mrazo_memory-traces_objectuse_v01

defaults:
  - cogeval_defaults

game:
  num_agents: 4

  map_builder:
    _target_: mettagrid.config.room.room_list.RoomList
    rooms:
      - _target_: mettagrid.config.room.random.Random
      width: ${uniform:25,60,30}
      height: ${uniform:25,60,30}
      border_width: ${uniform:1,6,3}
      agents: 1
      objects:
        mine: ${uniform:30,60,30}
        generator: ${uniform:30,60,30}
        altar: ${uniform:30,60,30}
        wall: ${uniform:30,60,30}
     - 
name: nav_mem_curriculum
algorithm: discrete_random

# Use this environment pretrained on a policy that is the best at navigation sequence already
# gd_backchain_mem_pretained:v18
children:
  - /env/mettagrid/curriculum/navigation/prioritize_regressed
  - /env/mettagrid/curriculum/navsequence/mem_medium
  # - /env/mettagrid/curriculum/navsequence/mem_hard
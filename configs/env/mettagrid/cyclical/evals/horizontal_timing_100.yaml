defaults:
  - /env/mettagrid/cyclical/evals/defaults@
  - _self_

game:
  max_steps: 500

  agent:
    rewards:
      inventory:
        heart: 0.2000000000

  objects:
    converter:
      conversion_ticks: 100
      cooldown: 100
    C:
      conversion_ticks: 100
      cooldown: 100
      phase: 100

  map_builder:
    uri: "configs/env/mettagrid/maps/cyclical/cyclical_horizontal_hallway.map"

# @package __global__

# This file is private property of Greg.
# DO NOT MODIFY.

run: gregorypylypovych_2025_08_12_training_process_2
# seed: 0
# sim: navigation
trainer:
  simulation:
    skip_git_check: true
    evaluate_interval: 10
    replay_dir: wandb://metta/replays/${run}
    # evaluate_remote: false
  curriculum: env/mettagrid/curriculum/autocurricula/random
  # ppo:
  #   gamma: 0.999
  # minibatch_size: 16384
  checkpoint:
    checkpoint_interval: 10
    wandb_checkpoint_interval: 10

wandb:
  enabled: true

replay_job:
  sim:
    env: env/mettagrid/autocurricula/evals/big_agent_box_playing_grid
    # env: env/mettagrid/autocurricula/defaults
    env_overrides:
      game:
        no_agent_interference: true


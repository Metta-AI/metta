# @package __global__

defaults:
  # - override /agent: simple_with_memory
  # - override /sim: t_maze
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/t_maze
  wandb.enabled: true
  wandb.track: true

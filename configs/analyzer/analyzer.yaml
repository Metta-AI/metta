_target_: rl.eval.eval_stats_analyzer.EvalStatsAnalyzer

policy_uri: null

analysis:

  filters: [] #global filters

  metric_patterns: null

  metrics:
    - metric: "action.use.altar.agent"

  queries: null

  baseline:
    filters:
      policy_name: null #if null, use all policies
      '"action.use.altar"' : "> 0"
_target_: metta.mettagrid.curriculum.bucketed.BucketedCurriculum

env_cfg_template: /env/mettagrid/simple

buckets:
  game.map_builder.room.width:
    range: [20, 60]
  game.map_builder.room.height:
    range: [20, 60]
  game.agent.rewards.ore_red:
    values: [0, 0.5, 1]
  game.agent.rewards.battery_red:
    values: [0, 0.5, 1]
  game.agent.rewards.heart:
    values: [0, 0.5, 1]

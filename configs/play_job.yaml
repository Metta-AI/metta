defaults:
  - common
  - wandb: metta_research
  - /sim/simple@play_job.sim
  - _self_

cmd: play
run: ???
data_dir: ./train_dir
run_dir: ${data_dir}/${run}
eval_db_uri: null

torch_deterministic: true


play_job:
  policy_uri: ${policy_uri}

[bdist_wheel]
# Ensure the wheel is treated as platform-specific (non-pure)
universal = 0
# Optionally set platform tag when building locally; CI tools like cibuildwheel set this automatically.
# plat_name = macosx_11_0_arm64

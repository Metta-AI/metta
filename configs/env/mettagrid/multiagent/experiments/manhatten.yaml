defaults:
  - /env/mettagrid/multiagent/experiments/defaults@
  - _self_

game:
  map_builder:
    _target_: metta.mettagrid.room.multi_agent.manhatten.Manhatten
    width: 90  # Fixed value (was 90  # Fixed value (was ${sampling:60,120,90}))
    height: 90  # Fixed value (was 90  # Fixed value (was ${sampling:60,120,90}))
    corridor_spacing: 25  # Fixed value (was 25  # Fixed value (was ${sampling:15,40,25}))
    alcove_prob: 0.15  # Fixed value (was 0.15  # Fixed value (was ${sampling:0.05,0.3,0.15}))
    heart_prob: 0.1  # Fixed value (was 0.1  # Fixed value (was ${sampling:0.05,0.15,0.1}))
    objects:
      altar: 10  # Fixed value (was 10  # Fixed value (was ${sampling:5,15,10}))
      mine_red: 10  # Fixed value (was 10  # Fixed value (was ${sampling:5,15,10}))
      generator_red: 10  # Fixed value (was 10  # Fixed value (was ${sampling:5,15,10}))

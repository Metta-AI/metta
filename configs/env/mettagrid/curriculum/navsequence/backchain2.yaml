_target_: mettagrid.curriculum.BucketedCurriculum

env_cfg_template: /env/mettagrid/extended_sequence/backchain2

buckets:
  game.map_builder.room.dir:
    values:
      [
        "varied_terrain/sparse_small",
        "varied_terrain/balanced_small",
        "varied_terrain/dense_small",
      ]
  # game.objects.altar.cooldown:
  #   range: [15, 100]
  # game.objects.altar.initial_items:
  #   values: [0, 1]
  # game.map_builder.room.objects.generator.red:
  #   range: [1, 3]
  # game.map_builder.room.objects.mine.red:
  #   range: [1, 3]
  game.objects.generator.red.cooldown:
    range: [15, 100]

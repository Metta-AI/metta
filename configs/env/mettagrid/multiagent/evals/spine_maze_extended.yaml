defaults:
  - /env/mettagrid/multiagent/evals/defaults@
  - _self_

game:
  num_agents: 6 #how many agents are in the map x2
  max_steps: 70
  agent:
    rewards:
      heart: 1
  map_builder:
    uri: "configs/env/mettagrid/maps/multiagent/spine_maze_extended.map"

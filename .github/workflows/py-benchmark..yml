name: py-benchmark

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]
    branches: [main]
    paths:
      - '**.py'
      - 'requirements.txt'
      - 'setup.py'
      - 'tests/**'
      - 'benchmarks/**'
      - 'deps/mettagrid/tests/**'
      - 'deps/mettagrid/benchmarks/**'
  push:
    branches: [main]
    paths:
      - '**.py'
      - 'requirements.txt'
      - 'setup.py'
      - 'tests/**'
      - 'benchmarks/**'
      - 'deps/mettagrid/tests/**'
      - 'deps/mettagrid/benchmarks/**'
  workflow_dispatch: {}


jobs:
  py-benchmark:
    if: github.event.pull_request.draft == false || github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      checks: write
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: ./.github/actions/py-setup.yml

      - name: Build C extensions (if needed)
        run: |
          source venv/bin/activate
          python setup.py build_ext --inplace

      - name: Run Python benchmarks
        env:
          HYDRA_FULL_ERROR: 1
          PYTHONPATH: ${{ github.workspace }}
        run: |
          source venv/bin/activate
          pytest tests --benchmark-json=benchmark_results.json

      - name: Install Bencher CLI
        uses: bencherdev/bencher@main

      - name: Validate Bencher Token
        env:
          BENCHER_API_TOKEN: ${{ secrets.BENCHER_API_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Bencher token length: ${#BENCHER_API_TOKEN}"
          echo "Bencher token SHA256: $(echo -n "$BENCHER_API_TOKEN" | sha256sum)"

      - name: Upload to Bencher
        env:
          BENCHER_API_TOKEN: ${{ secrets.BENCHER_API_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH_NAME: ${{ github.ref == 'refs/heads/main' ? 'master' : (GITHUB_HEAD_REF != '' ? GITHUB_HEAD_REF : 'manual-run') }}
        if: github.ref == 'refs/heads/main' || (github.event_name == 'pull_request' && !github.event.pull_request.head.repo.fork) || github.event_name == 'workflow_dispatch'
        run: |
          bencher run \
            --project mettagrid-sv3f5i2k \
            --token "$BENCHER_API_TOKEN" \
            --branch "$BRANCH_NAME" \
            --testbed ubuntu-latest \
            --adapter python_pytest \
            --github-actions "$GITHUB_TOKEN" \
            --file benchmark_results.json > /dev/null

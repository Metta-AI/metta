# @package __global__

defaults:
  - override /agent: robust_cross
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/memory
  wandb.enabled: true
  wandb.track: true

run: mrazo_act-obs_memory_robust-cross_v04

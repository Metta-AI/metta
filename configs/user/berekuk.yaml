# @package __global__

defaults:
  - _self_

run_id: 1
run: ${oc.env:USER}.local.${run_id}

policy_uri: wandb://run/${run}

trainer:
  env: /env/mettagrid/mapgen_auto

  # env_overrides:
  #   sampling: 0.7
  #   game:
  #     num_agents: 36
  #     max_steps: 1000

_target_: metta.mettagrid.curriculum.bucketed.BucketedCurriculum

env_cfg_template_path: /env/mettagrid/cooperation/experimental/central_table_layout_template

buckets:
  game.params.max_steps:
    range: [100, 500]
  game.params.width:
    range: [5, 10]
  game.params.height:
    range: [5, 10]
  game.params.num_generators:
    range: [1, 4]
  game.params.num_altars:
    range: [1, 4]
  game.params.num_mines:
    range: [1, 4]
  game.params.altar_cooldown:
    range: [10, 25]
  game.params.altar_conversion_ticks:
    range: [10, 25]
  game.params.generator_cooldown:
    range: [10, 25]
  game.params.generator_conversion_ticks:
    range: [10, 25]
  game.params.mine_cooldown:
    range: [10, 25]

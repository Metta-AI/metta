# @package __global__

defaults:
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/navigation/learning_progress

policy_uri: wandb://run/sasmith-nav20250709
replay_job:
  sim:
    env: /env/mettagrid/navigation/training/sparse_medium

seed: null
run_id: 20250709.01
run: ${oc.env:USER}.local.${run_id}

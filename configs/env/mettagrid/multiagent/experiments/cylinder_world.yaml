defaults:
  - /env/mettagrid/multiagent/experiments/defaults@
  - _self_

# sampling: 1  # Removed - using fixed values instead

game:
  map_builder:
    _target_: metta.mettagrid.room.navigation.cylinder_world_sequence.CylinderWorldSequence
    objects:
      altar: 20  # Fixed value (was ${sampling:15,25,20})
      mine_red: 20  # Fixed value (was ${sampling:15,25,20})
      generator_red: 20  # Fixed value (was ${sampling:15,25,20})

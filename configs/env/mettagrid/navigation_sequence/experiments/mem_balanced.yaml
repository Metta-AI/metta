# 'mem' environments are small environments with few objects, the objects have relatively short cooldowns, so the agent needs to remember where objects are located as they need to revisit objects multiple times. the all have the full sequence mine-generator-altar


defaults:
  - /env/mettagrid/navigation_sequence/training/mem_defaults@
  - _self_

game:
  map_builder:
    room:
      style: balanced

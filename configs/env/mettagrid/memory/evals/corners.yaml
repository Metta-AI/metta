defaults:
  - /env/mettagrid/mettagrid@

game:
  num_agents: 4  #how many agents are in the map x2
  max_steps: 150
  agent:
    rewards:
      heart: 1
  map_builder:

    _target_: mettagrid.room.multi_room.MultiRoom
    num_rooms: 4
    room:
      _target_: mettagrid.room.ascii.Ascii
      border_width: 1
      uri: "configs/env/mettagrid/maps/memory/corners.map"

  objects:
    altar:
      cooldown: 255

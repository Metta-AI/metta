defaults:
  - common
  - agent: simple
  - trainer: puffer
  - eval: all
  - analyzer: eval_analyzer
  - wandb: metta_research
  - _self_

cmd: eval
eval_db_uri: null

torch_deterministic: true
vectorization: multiprocessing

defaults:
  - /env/mettagrid/multiagent/experiments/defaults_bucketed@
  - _self_

env_overrides:
  game:
    map_builder:
      _target_: metta.mettagrid.room.multi_agent.narrow_world.NarrowWorld
      border_width: 0
      objects:
        altar: 40
        mine_red: 40
        generator_red: 40

# @package __global__

defaults:
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/simple
  wandb.enabled: true
  wandb.track: true

# replay_job:
#   policy_uri: wandb://run/mrazo_action-obs_memory_test01
#   wandb.enabled: false
#   wandb.track: false

run: mrazo_action-obs_simple_v001

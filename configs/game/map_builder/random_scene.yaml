# Test config for random scene.
_target_: mettagrid.map.mapgen.MapGen

width: 40
height: 40

root:
  _target_: mettagrid.map.scenes.random_scene.RandomScene

  candidates:
    - scene: /scenes/convchain/dungeon
      weight: 2
    - scene: /scenes/convchain/blob
      weight: 1

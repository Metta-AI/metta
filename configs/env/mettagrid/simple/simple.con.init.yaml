defaults:
  - simple
  - _self_

# Converters start with items, and only require one input item.
game:
  objects:
    altar:
      input_battery_red: 1
      initial_items: 1
    mine_red:
      initial_items: 1
    generator_red:
      initial_items: 1

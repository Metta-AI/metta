defaults:
  - navigation_defaults

game:
  num_agents: 2  #how many agents are in the map x2

  map_builder:

    _target_: mettagrid.config.room.multi_room.MultiRoom
    num_rooms: 2
    room:
      _target_: mettagrid.config.room.ascii.Ascii
      border_width: 1
      uri: "configs/env/mettagrid/maps/decathlon5.map"

  objects:
    mine.red:
      max_output: 1
      cooldown: 1000
    generator.red:
      max_output: 1
      cooldown: 1000
    altar.red:
      max_output: 1
      cooldown: 1000
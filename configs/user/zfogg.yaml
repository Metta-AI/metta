# @package __global__

defaults:
  # - /common
  # - /agent/fast
  # - /sim/all
  # - /sim/arena@sim
  # - /sim/navigation@sim
  # - /wandb/metta_research
  - _self_

trainer:
  total_timesteps: 10_000_000
  # curriculum: /env/mettagrid/navigation/training/multienv
  # curriculum: /env/mettagrid/curriculum/arena/learning_progress
  checkpoint:
    checkpoint_interval: 5
    wandb_checkpoint_interval: 5
  simulation:
    evaluate_interval: 10 # must be ≥ both checkpoint intervals (policies needed for eval)
    evaluate_remote: true
    skip_git_check: true

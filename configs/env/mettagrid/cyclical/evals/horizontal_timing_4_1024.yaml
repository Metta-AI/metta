defaults:
  - /env/mettagrid/cyclical/evals/defaults@
  - _self_

game:
  max_steps: 500

  objects:
    converter:
      conversion_ticks: 4
      cooldown: 4
    C:
      conversion_ticks: 4
      cooldown: 4
      phase: 4

  map_builder:
    uri: "configs/env/mettagrid/maps/cyclical/cyclical_horizontal_hallway_1024.map"

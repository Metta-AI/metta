# Base trainer configuration
# Most settings are now in separate configs:
# - /ppo: PPO algorithm parameters
# - /optimizer: Optimizer settings
# - /training: Training and experience settings
# - /environment: Environment and vecenv settings
# - /kickstarter: Imitation learning settings

# This file should only contain settings specific to the trainer class itself


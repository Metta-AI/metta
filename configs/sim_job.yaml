defaults:
  - common
  - wandb: metta_research
  - /sim/simple@sim_job.simulation_suite
  - _self_

cmd: sim
run: ???
data_dir: ./train_dir
run_dir: ${data_dir}/${run}
torch_deterministic: true

sim_job:
  simulation_suite:
    run_dir: ${run_dir}
  policy_uris:
    - ${policyx_uri}
  selector_type: latest

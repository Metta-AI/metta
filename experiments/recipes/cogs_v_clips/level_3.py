# with clipping
# need to be getting other things out of the assembler


# ClipperConfig
# what you need to unclip the thing that has been clipped
# unclipping recipe

# clip-rate: probability on every timestep that one additional assembler is clipped (number of assemblers / number of game steps)

# env.game.clipper = ClipperConfig(recipe, length_scale, cutoff_distance, clip_rate)

# length scale: how far away from the assembler that can be clipped

# cutoff distance: how far away from the assembler that can be clipped

# recipe: the recipe that is used to unclip the assembler

apiVersion: v1
kind: ConfigMap
metadata:
  name: home-config
data:
  config.json: |
    {
      "host": "{{ .Values.host }}",
      "links": {{ (.Files.Get "links.yaml" | fromYaml).links | toJson }}
    }

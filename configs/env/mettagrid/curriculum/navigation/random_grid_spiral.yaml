_target_: metta.mettagrid.curriculum.random.RandomCurriculum

tasks:
  # Grid navigation pattern
  /env/mettagrid/navigation/training/grid_multiroom: 0.5

  # Spiral navigation pattern
  /env/mettagrid/navigation/training/spiral_altar_multiroom: 0.5

# Equal probability for both task types
# Each task uses random parameter sampling internally

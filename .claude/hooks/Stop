#!/bin/bash

# Run ruff format on all Python files
echo "Running ruff format..."
uv run ruff format .

# Run ruff check with fix and unsafe-fixes
echo "Running ruff check with fixes..."
uv run ruff check --fix --unsafe-fixes .

echo "Ruff formatting and checking complete!"
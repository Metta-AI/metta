# W&B configuration for comparison trainer project
enabled: true

project: comparision_trainer
entity: metta-research
group: ${run}
name: ${run}
run_id: ${run}

data_dir: ${run_dir}

job_type: ${cmd}
tags: []
notes: "" 
_target_: metta.mettagrid.curriculum.random.RandomCurriculum

tasks:
  # Single bucket - 4 rooms with randomly sampled raster grid parameters each episode
  /env/mettagrid/navigation/evals/raster_grid_multiroom_random: 1.0

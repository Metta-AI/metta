# @package __global__

# @package __global__

# trainer:
#   env: /env/mettagrid/diversity/multi_left_or_right

run: lars_sim_test_2

policy_uri: wandb://run/sasmith.20250610.bisect.devbox

# Mind reader configuration for collecting LSTM memory and position data
mind_reader:
  enabled: true
  output_dir: ./mind_reader/data/raw_data/

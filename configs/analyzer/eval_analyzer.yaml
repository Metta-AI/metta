_target_: rl.eval.eval_stats_analyzer.EvalStatsAnalyzer

policy_uri: ???

view_type: policy_versions # this is what we want when training a policy
output_path: /tmp/dashboard.html
metric: episode_reward

analysis:

  metric_patterns: null

  metrics:
    - metric: episode_reward

  queries: null

  baseline_policies: null

  log_all: true

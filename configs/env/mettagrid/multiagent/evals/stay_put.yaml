defaults:
  - /env/mettagrid/multiagent/evals/defaults@
  - _self_

game:
  num_agents: 4 #how many agents are in the map x2
  max_steps: 40
  agent:
    rewards:
      heart: 0.25
  map_builder:
    uri: "configs/env/mettagrid/maps/multiagent/stay_put.map"

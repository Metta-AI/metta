defaults:
  - common
  - agent: simple
  - trainer: puffer
  - env/mettagrid@env: simple
  - eval: eval_suite
  - analyzer: train_analyzer
  - wandb

seed: 1

policy_uri: ./train_dir/${.run}/checkpoints/

eval:
  env: env/mettagrid/simple
  num_envs: 25
  num_episodes: 25
  max_time_s: 600
  policy_agents_pct: 1.0
  eval_db_uri: ${..run_dir}/eval_stats.json

defaults:
  - /env/mettagrid/mettagrid@
  - _self_

sampling: 1

game:
  num_agents: 4

  agent:
    ore.red_max: 255
    battery.red_max: 255
    heart_max: 255
    rewards:
      battery: 1

  map_builder:
    _target_: mettagrid.room.multi_room.MultiRoom
    num_rooms: 4
    border_width: 6

    room:
      _target_: mettagrid.room.navigation.varied_terrain.VariedTerrain
      width: ${sampling:50,100,75}
      height: ${sampling:50,100,75}
      border_width: ${sampling:1,6,3}
      agents: 1
      style: all-sparse
      objects:
        altar: 0
        generator.red: ${sampling:20,50,30}
        mine.red: 0
  objects:
    generator.red:
      cooldown: 255
      input_ore.red: 1
      output_battery: 1
      max_output: 1
      conversion_ticks: 1
      initial_items: 1

# @package __global__

defaults:
  - /common
  - /agent/fast
  - /trainer/trainer
  - /sim/coop_rewardsharing@sim
  - _self_

run: krishnakanth.coop_rewardsharing.lp
cmd: train

data_dir: ${oc.env:DATA_DIR,./train_dir}
run_dir: ${data_dir}/${run}

trainer:
  curriculum: /env/mettagrid/curriculum/coop_rewardsharing/learning_progress
  num_workers: 4
  simulation:
    evaluate_interval: 50
    skip_git_check: true
  checkpoint:
    wandb_checkpoint_interval: 50
  total_timesteps: 10_000_000_000

wandb:
  enabled: true
  project: metta
  entity: metta-research
  group: ${run}
  name: ${run}
  run_id: ${run}
  data_dir: ${run_dir}
  job_type: ${cmd}

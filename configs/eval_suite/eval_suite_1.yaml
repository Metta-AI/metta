defaults:
  - ../evaluator/evaluator.yaml

policy_uri: "wandb://run/v.daveey.train.sm.teams.100"
baseline_uri: ["wandb://run/v.daveey.train.sm.teams.75", "wandb://run/v.daveey.train.sm.teams.25"]

evals:
  - env: env/mettagrid/cylinder
    metrics_to_log:
      - time_to_targets
      - elo
    npc_policy_uri: null

  - env: env/mettagrid/maze
    metrics_to_log:
      - time_to_targets
      - elo
    npc_policy_uri: null

  - env: env/mettagrid/simple
    metrics_to_log:
      - elo
      - gleicko
    npc_policy_uri: "wandb://run/v.daveey.train.sm.teams.50"
  - env: env/mettagrid/simple
    metrics_to_log:
      - elo
      - gleicko
    npc_policy_uri: "wandb://run/v.daveey.train.sm.teams.75"

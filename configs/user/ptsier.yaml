# @package __global__

defaults:
  - override /sim: navigation
  - _self_

run: pasha_sim
policy_uri: wandb://run/daphne.regularnav.main:v2

stats_server_uri: http://localhost:8000
stats_user: ptsier

sim_job:
  stats_db_uri: postgres://postgres:password@127.0.0.1/postgres


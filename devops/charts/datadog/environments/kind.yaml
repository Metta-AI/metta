datadog:
  datadog:
    clusterName: "kind-metta-local"
    tags:
      - "env:local"
  env: "local"
  agents:
    containers:
      agent:
        env:
          - name: DD_HOSTNAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
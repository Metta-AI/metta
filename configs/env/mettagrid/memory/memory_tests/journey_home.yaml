defaults:
  - /env/mettagrid/memory/evals/defaults@

game:
  max_steps: 400
  map_builder:
    uri: "configs/env/mettagrid/maps/memory_tests/journey_home.map"
  objects:
    altar:
      initial_items: 1

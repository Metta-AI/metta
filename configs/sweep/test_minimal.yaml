# Minimal test configuration for e2e testing
# Single parameter, minimal timesteps

parameters:
  # Just optimize learning rate
  trainer.learning_rate:
    min: 0.0001
    max: 0.001
    distribution: log_normal

# Optional metadata (will use defaults if not specified)
metric: reward
goal: maximize

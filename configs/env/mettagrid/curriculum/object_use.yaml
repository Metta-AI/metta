name: object_use_curriculum
algorithm: prioritize_regressed

# This is a hierarchical curriculum that references object sub-curricula
children:
  - /env/mettagrid/curriculum/objects/all_objects
  - /env/mettagrid/curriculum/objects/sequence

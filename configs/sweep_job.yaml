defaults:
  - common
  - wandb: metta_research
  - agent: fast
  - sim: sweep_eval
  - trainer: trainer
  - sweep: quick
  - _self_

cmd: sweep
sweep_run: ???

sweep_job:
  sim: ${sim}
  trainer: ${trainer}
  agent: ${agent}
  wandb: ${wandb}
  runs_dir: "${sweep_dir}/runs"
  train_job: # TODO: Not certain we need this.
    evals: ${sim}
  seed: null  # Will be randomly generated for each run if not specified

sweep_dir: "${.data_dir}/sweep/${.sweep_run}"
runs_dir: "${.sweep_dir}/runs"

image:
  repository: metta-local
  tag: "latest"
  pullPolicy: Never

env:
  BACKEND_URL: "http://host.docker.internal:8000"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi


defaults:
  - /env/mettagrid/cyclical/evals/defaults@
  - _self_

game:
  max_steps: 500

  agent:
    rewards:
      inventory:
        heart: 0.0303030303

  objects:
    converter:
      conversion_ticks: 15
      cooldown: 15
    C:
      conversion_ticks: 15
      cooldown: 15
      phase: 15

  map_builder:
    uri: "configs/env/mettagrid/maps/cyclical/cyclical_horizontal_hallway.map"

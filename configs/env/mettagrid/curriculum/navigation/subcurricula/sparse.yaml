name: sparse

env_paths:
  - /env/mettagrid/navigation/training/sparse

parameters:
  game.map_builder.width: { range: [60, 120] }
  game.map_builder.height: { range: [60, 120] }
  game.map_builder.root.params.objects.altar: { range: [1, 10] }

# @package __global__

# This file is private property of Greg.
# DO NOT MODIFY.

run: gregorypylypovych_2025_08_19_run
# seed: 0
# sim: navigation
trainer:
  simulation:
    skip_git_check: true
    # evaluate_interval: 5
    # evaluate_remote: false
  curriculum: env/mettagrid/curriculum/autocurricula/random
  # ppo:
  #   gamma: 0.999
  # minibatch_size: 16384
  # checkpoint:
  #   checkpoint_interval: 5
  #   wandb_checkpoint_interval: 5

replay_job:
  sim:
    env: env/mettagrid/autocurricula/obstacles0 #env/mettagrid/autocurricula/defaults
    env_overrides:
      game:
        no_agent_interference: true


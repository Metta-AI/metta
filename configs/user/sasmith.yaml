# @package __global__

defaults:
  - _self_

trainer:
  curriculum: /env/mettagrid/curriculum/navigation/learning_progress


policy_uri: wandb://run/sasmith-nav20250710
replay_job:
  sim:
    curriculum: /env/mettagrid/navigation/training/sparse_bucketed
  replay_dir: s3://softmax-public/replays/${run}

seed: null
run_id: 20250712.02
run: ${oc.env:USER}.local.${run_id}

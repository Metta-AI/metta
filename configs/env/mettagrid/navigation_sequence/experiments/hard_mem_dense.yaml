_target_: metta.mettagrid.curriculum.bucketed.BucketedCurriculum

env_cfg_template_path: /env/mettagrid/navigation_sequence/experiments/hard_mem_defaults

buckets:
  game.objects.altar.initial_resource_count:
    - [0, 0, 0, 1]
  game.objects.generator_red.initial_resource_count:
    - [0, 0, 0, 1]

env_overrides:
  game:
    map_builder:
      room:
        dir: varied_terrain/dense_small

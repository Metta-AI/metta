# @package __global__

seed: null

defaults:
  # - override /env/mettagrid@env: simple
  - override /agent: latent_attn_small
  - _self_

# policy_uri: pytorch:///tmp/puffer_metta.pt
policy_uri: wandb://run/m_alexv_ks_dr_lam_001:v22 #wandb://run/b_alex_ks_ndr_student_017:v2
npc_policy_uri: null

trainer:
  initial_policy:
    uri: wandb://run/b_alex_ks_ndr_student_017:v2

# env_overrides:
#   game:
#     max_steps: 1000

# env:
#   game:
#     max_steps: 1000

eval:
  policy_uri: ${..policy_uri}
  npc_policy_uri: ${..npc_policy_uri}
  # eval_db_uri: ${..eval_db_uri}
  # env: /env/mettagrid/prog_3.yaml
  # num_episodes: 10
  # max_time_s: 60

sweep:
  metric: "action.use.altar.agent"

wandb:
  enabled: true
  # checkpoint_interval: 1

run_id: 1358
run: ${oc.env:USER}.local.${run_id}
trained_policy_uri: ${run_dir}/checkpoints
sweep_name: "${oc.env:USER}.local.sweep.${run_id}"

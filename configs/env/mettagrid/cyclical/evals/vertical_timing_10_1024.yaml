defaults:
  - /env/mettagrid/cyclical/evals/defaults@
  - _self_

game:
  max_steps: 500

  objects:
    converter:
      conversion_ticks: 10
      cooldown: 10
    C:
      conversion_ticks: 10
      cooldown: 10
      phase: 10

  map_builder:
    uri: "configs/env/mettagrid/maps/cyclical/cyclical_vertical_hallway_1024.map"

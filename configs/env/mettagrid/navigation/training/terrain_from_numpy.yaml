defaults:
  - /env/mettagrid/navigation/training/defaults@
  - _self_

game:
  num_agents: 4
  map_builder:
    room:
      dir: terrain_maps_nohearts
  objects:
    altar:
      cooldown: 1000
      input_battery.red: 3
      output_heart: 1
      max_output: 5
      conversion_ticks: 1
      initial_items: 1

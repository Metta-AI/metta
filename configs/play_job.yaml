defaults:
  - common
  - wandb: metta_research
  - sim: sim_single
  - _self_

cmd: play
run: ???
run_dir: ${data_dir}/${run}

torch_deterministic: true

play_job:
  sim: ${sim}
  policy_uri: ${policy_uri}

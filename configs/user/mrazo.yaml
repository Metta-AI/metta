# @package __global__

defaults:
  - _self_

trainer:
  env: env/mettagrid/ensemble
  wandb.enabled: true
  # initial_policy:
  #   uri: wandb://run/daphne_objectuse_allobjs_multienv:v94
  # evaluate_interval: 100
  # checkpoint_interval: 100

# replay_job:
#   policy_uri: wandb://run/daphne_objectuse_allobjs_multienv:v94
#   # wandb.enabled: false
#   wandb.track: false

run: mrazo_nav-obj-mem_ensemble_v01
name: "Deploy PR Similarity Cache CronJob"
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
    paths:
      - "mcp_servers/pr_similarity/**"
      - "packages/gitta/**"
      - "devops/charts/cronjob/**"
      - ".github/workflows/deploy-pr-similarity-cache-cronjob.yml"
  workflow_dispatch:
    inputs:
      dev:
        description: "Deploy as dev variant (uses -dev suffix and separate release)"
        required: false
        type: boolean
        default: false
      branch:
        description: "Branch to build from (defaults to current branch)"
        required: false
        type: string
        default: ""

jobs:
  build-and-push-image:
    runs-on: ubuntu-4core
    timeout-minutes: 20
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch || github.ref }}
          persist-credentials: false

      - name: Build Docker image
        id: build-image
        uses: ./.github/actions/docker-build
        with:
          image_name: metta-cronjobs
          suffix: ${{ inputs.dev && '-pr-similarity-dev' || '-pr-similarity' }}
          dockerfile: ./devops/charts/cronjob/Dockerfile.pr-similarity
          aws_role: ${{ vars.AWS_ROLE }}

      - name: Configure EKS
        if: github.ref == 'refs/heads/main' || inputs.dev
        uses: ./.github/actions/eks-configure

      - name: Deploy with Helm
        if: github.ref == 'refs/heads/main' || inputs.dev
        uses: ./.github/actions/helm-deploy
        with:
          namespace: monitoring
          release: ${{ inputs.dev && 'pr-similarity-cache-cronjob-dev' || 'pr-similarity-cache-cronjob' }}
          chart: ./devops/charts/cronjob
          image_tag: ${{ steps.build-image.outputs.tag }}

_target_: metta.mettagrid.curriculum.colortree_random.ColorTreeRandomFromSetCurriculum

tasks:
  /env/mettagrid/colortree_easy: 1.0

# Override sequence parameters
sequence_length: 2  # Use sequences of length 2
# num_colors: 2  # Optional: override number of colors (auto-detected if not specified)
